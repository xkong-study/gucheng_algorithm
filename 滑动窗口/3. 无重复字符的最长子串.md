```code
var lengthOfLongestSubstring = function(s) {
let set = new Set();
let max = 0;
let start = 0;
for(let i=start;i<s.length;i++){
    while(set.has(s[i])){
        set.delete(s[start]);
        start++;
    }
    max = Math.max(max,i-start+1);
    set.add(s[i]);
}
return max;
};
```
